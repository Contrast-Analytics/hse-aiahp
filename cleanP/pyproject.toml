[tool]
[tool.poetry]
authors = ["tararonis <tararonis@gmail.com>"]
description = ""
name = "cleanp"
readme = "README.md"
version = "0.1.0"

[tool.poetry.dependencies]
python = "^3.10"

[build-system]
build-backend = "poetry.core.masonry.api"
requires = ["poetry-core"]
